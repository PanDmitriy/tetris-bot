# 🚗 Tetris Racing Bot

Telegram бот с игрой в гонки в стиле Тетриса. Управляйте машинкой, избегайте падающих блоков и набирайте очки!

## 🛠 Технологии

### Backend
- **Node.js** + **TypeScript**
- **grammy** - фреймворк для Telegram ботов
- **Prisma** - ORM для работы с PostgreSQL
- **Express** - веб-сервер

### Frontend
- **React 18** + **TypeScript**
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **Framer Motion** - анимации
- **Zustand** - управление состоянием
- **Canvas API** - игровой движок

### Инфраструктура
- **Docker** + **Docker Compose** - контейнеризация
- **PostgreSQL** - база данных
- **Nginx** - веб-сервер для фронтенда

## 📋 Требования

- Node.js >= 18.0.0
- npm >= 9.0.0
- Docker и Docker Compose (для деплоя)
- PostgreSQL (для локальной разработки без Docker)

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
# Установка зависимостей
npm install
```

### 2. Настройка переменных окружения

```bash
# Скопируйте пример файла
cp .env.example .env

# Отредактируйте .env и укажите:
# - BOT_TOKEN (получите у @BotFather в Telegram)
# - DATABASE_URL (если используете локальную БД)
# - WEBAPP_URL (URL вашего фронтенда)
```

### 3. Настройка базы данных

```bash
# Генерация Prisma клиента
npm run db:generate

# Применение миграций
npm run db:migrate
```

### 4. Запуск в режиме разработки

```bash
# Backend (в одном терминале)
npm run dev:bot

# Frontend (в другом терминале)
npm run dev:webapp
```

### 5. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен и добавьте в `.env`
3. Настройте WebApp:
   ```
   /newapp
   Выберите вашего бота
   Название: Tetris Racing
   Описание: Игра в гонки
   Фото: (опционально)
   Web App URL: http://localhost:5173 (для разработки)
   ```

## 🐳 Деплой с Docker

### 1. Подготовка

```bash
# Создайте .env файл с production значениями
cp .env.example .env
# Отредактируйте .env
```

### 2. Запуск

```bash
# Сборка и запуск всех сервисов
docker-compose up -d

# Применение миграций БД
docker-compose exec backend npm run db:deploy

# Просмотр логов
docker-compose logs -f
```

### 3. Остановка

```bash
docker-compose down
```

## 📁 Структура проекта

```
tetris-bot/
├── backend/              # Backend приложение
│   ├── src/
│   │   ├── index.ts     # Точка входа
│   │   ├── db.ts        # Prisma клиент
│   │   ├── commands.ts  # Команды бота
│   │   └── handlers.ts  # Обработчики событий
│   ├── prisma/
│   │   └── schema.prisma # Схема БД
│   └── package.json
├── frontend/            # Frontend приложение
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── engine/      # Игровой движок
│   │   ├── store/       # Zustand store
│   │   └── App.tsx
│   └── package.json
├── docker-compose.yml   # Docker конфигурация
└── package.json        # Root package.json
```

## 🎮 Игровые механики

- **Управление**: Стрелки влево/вправо или свайпы
- **Цель**: Избегать падающих блоков
- **Очки**: Начисляются за пройденное расстояние и избежание блоков
- **Сложность**: Увеличивается со временем (блоки падают быстрее)

## 🔧 Команды разработки

```bash
# Разработка
npm run dev:bot          # Запуск backend
npm run dev:webapp       # Запуск frontend

# Сборка
npm run build            # Сборка всех проектов
npm run build:bot        # Сборка только backend
npm run build:webapp     # Сборка только frontend

# База данных
npm run db:generate      # Генерация Prisma клиента
npm run db:migrate       # Применение миграций
npm run db:studio        # Открыть Prisma Studio

# Линтинг и форматирование
npm run lint             # Проверка кода
npm run format           # Форматирование кода
```

## 📊 База данных

### Модели

- **User** - пользователи Telegram
- **GameResult** - результаты игр
- **UserStats** - статистика пользователей

### Миграции

```bash
# Создать новую миграцию
npm run db:migrate --workspace=backend

# Применить миграции в production
npm run db:deploy --workspace=backend
```

## 🌐 Production деплой на VPS

### 1. Подготовка сервера

```bash
# Установите Docker и Docker Compose
sudo apt update
sudo apt install docker.io docker-compose

# Клонируйте репозиторий
git clone <your-repo>
cd tetris-bot
```

### 2. Настройка

```bash
# Создайте .env файл
cp .env.example .env
nano .env  # Отредактируйте значения

# Укажите:
# - BOT_TOKEN
# - WEBAPP_URL (ваш домен или IP)
# - POSTGRES_PASSWORD (надежный пароль)
```

### 3. Запуск

```bash
# Сборка и запуск
docker-compose up -d

# Применение миграций
docker-compose exec backend npm run db:deploy

# Настройка Nginx (опционально, для домена)
# Создайте конфиг для проксирования на порты 3000 и 5173
```

### 4. Обновление

```bash
git pull
docker-compose build
docker-compose up -d
docker-compose exec backend npm run db:deploy
```

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте `BOT_TOKEN` в `.env`
- Убедитесь, что бот запущен: `npm run dev:bot`

### WebApp не открывается
- Проверьте `WEBAPP_URL` в `.env`
- Убедитесь, что фронтенд запущен: `npm run dev:webapp`
- Проверьте настройки WebApp в @BotFather

### Ошибки базы данных
- Убедитесь, что PostgreSQL запущен
- Проверьте `DATABASE_URL` в `.env`
- Примените миграции: `npm run db:migrate`

## 📝 TODO

- [ ] Реализация статистики пользователей
- [ ] Таблица лидеров
- [ ] Мультиплеер режим
- [ ] Улучшение графики
- [ ] Звуковые эффекты
- [ ] Достижения
- [ ] Ежедневные задания

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано с ❤️ для Telegram



